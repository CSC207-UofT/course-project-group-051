package demo2;

import java.util.ArrayList;

public class MessageView extends View{


    public MessageView(CurrentUser user, Database database) {
        super(user, database);
    }

    @Override
    public void run() {
//        int threadNum = 0;
//        int totalThreads;
//
//        totalThreads = showThreads();
//
//        if (totalThreads == -1) {
//            System.out.println("There are no messages to display.");
//        }
//
//        while (threadNum >= 0 && threadNum < )
//        System.out.println("Whose message thread would you like to open? Input the number corresponding to it.");
//        threadNum = scanner.nextInt();
//    }
//
//    public int showThreads() {
//        ArrayList<String> threads = user.displayThreads();
//
//        if (threads.isEmpty()) {
//            return -1;
//        } else {
//
//            String threadStack = threads.get(0);
//            threads.remove(0);
//            threadStack = i.toString() + ". " + threadStack;
//            for (String t : threads) {
//                i++;
//                threadStack = threadStack + "\n" + i.toString() + ". " + t;
//            }
//    }
//
//        System.out.println(threadStack);
//        return i;


    }

}
